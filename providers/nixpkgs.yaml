# Nixpkgs Provider Data - Nix package collection
version: "1.0"

provider:
  name: "nixpkgs"
  display_name: "Nixpkgs Collection"
  description: "The Nix packages collection"
  type: "package_manager"
  platforms: ["nixos", "linux", "macos"]
  executable: "nix-env"  # Main executable for availability detection
  capabilities: ["install", "uninstall", "upgrade", "search", "info", "list", "version", "start", "stop", "restart", "enable", "disable", "status", "logs"]

actions:
  install:
    description: "Install packages from Nixpkgs"
    template: "nix-env -f '<nixpkgs>' -iA {{sai_package(0, 'name', 'nixpkgs')}}"
    timeout: 600
    detection: "nix-env -q | grep {{sai_package(0, 'name', 'nixpkgs')}} >/dev/null 2>&1"
    validation:
      command: "nix-env -q | grep {{sai_package(0, 'name', 'nixpkgs')}}"
      expected_exit_code: 0
    rollback: "nix-env -e {{sai_package('*', 'name', 'nixpkgs')}}"

  uninstall:
    description: "Remove packages from Nixpkgs"
    template: "nix-env -e {{sai_package('*', 'name', 'nixpkgs')}}"
    detection: "nix-env -q | grep {{sai_package(0, 'name', 'nixpkgs')}} >/dev/null 2>&1"
    validation:
      command: "! nix-env -q | grep {{sai_package(0, 'name', 'nixpkgs')}}"
      expected_exit_code: 0

  upgrade:
    description: "Upgrade packages from Nixpkgs"
    steps:
      - name: "update-channel"
        command: "nix-channel --update"
      - name: "upgrade-packages"
        command: "nix-env -u {{sai_package('*', 'name', 'nixpkgs')}}"
    timeout: 600
    detection: "nix-env -q | grep {{sai_package(0, 'name', 'nixpkgs')}} >/dev/null 2>&1"

  start:
    description: "Start service via systemctl"
    template: "systemctl start {{sai_service(0, 'service_name', 'nixpkgs')}}"
    validation:
      command: "systemctl is-active {{sai_service(0, 'service_name', 'nixpkgs')}}"
      expected_output: "active"

  stop:
    description: "Stop service via systemctl"
    template: "systemctl stop {{sai_service(0, 'service_name', 'nixpkgs')}}"
    validation:
      command: "systemctl is-active {{sai_service(0, 'service_name', 'nixpkgs')}}"
      expected_output: "inactive"

  restart:
    description: "Restart service via systemctl"
    template: "systemctl restart {{sai_service(0, 'service_name', 'nixpkgs')}}"

  enable:
    description: "Enable service auto-start"
    template: "systemctl enable {{sai_service(0, 'service_name', 'nixpkgs')}}"

  disable:
    description: "Disable service auto-start"
    template: "systemctl disable {{sai_service(0, 'service_name', 'nixpkgs')}}"

  status:
    description: "Check service status"
    template: "systemctl status {{sai_service(0, 'service_name', 'nixpkgs')}}"

  logs:
    description: "Show service logs"
    template: "journalctl -u {{sai_service(0, 'service_name', 'nixpkgs')}} --no-pager -n 50"

  info:
    description: "Show package information"
    template: "nix-env -f '<nixpkgs>' -qa --description {{sai_package(0, 'name', 'nixpkgs')}}"

  search:
    description: "Search for packages"
    template: "nix-env -f '<nixpkgs>' -qa | grep {{sai_package(0, 'name', 'nixpkgs')}}"

  list:
    description: "List installed packages"
    template: "nix-env -q | grep {{sai_package(0, 'name', 'nixpkgs')}}"

  version:
    description: "Show package version"
    template: "nix-env -q {{sai_package(0, 'name', 'nixpkgs')}}"
