# DNF Provider Data - Fedora/RHEL package manager
version: "1.0"

provider:
  name: "dnf"
  display_name: "Dandified YUM"
  description: "Next-generation package manager for RPM-based systems"
  type: "package_manager"
  platforms: ["fedora", "rhel", "centos", "rocky", "alma"]
  executable: "dnf"  # Main executable for availability detection
  capabilities: ["install", "uninstall", "upgrade", "search", "info", "list", "version", "start", "stop", "restart", "enable", "disable", "status", "logs"]

actions:
  install:
    description: "Install packages via DNF"
    template: "dnf install -y {{sai_package('*', 'name', 'dnf')}}"
    timeout: 600
    detection: "dnf info {{sai_package(0, 'name', 'dnf')}} >/dev/null 2>&1"
    validation:
      command: "rpm -qa | grep {{sai_package(0, 'name', 'dnf')}}"
      expected_exit_code: 0
    rollback: "dnf remove -y {{sai_package('*', 'name', 'dnf')}}"

  uninstall:
    description: "Remove packages via DNF"
    template: "dnf remove -y {{sai_package('*', 'name', 'dnf')}}"
    detection: "rpm -qa | grep -q {{sai_package(0, 'name', 'dnf')}}"
    validation:
      command: "! rpm -qa | grep {{sai_package(0, 'name', 'dnf')}}"
      expected_exit_code: 0

  upgrade:
    description: "Upgrade packages via DNF"
    template: "dnf upgrade -y {{sai_package('*', 'name', 'dnf')}}"
    timeout: 600
    detection: "rpm -qa | grep -q {{sai_package(0, 'name', 'dnf')}}"

  start:
    description: "Start service via systemctl"
    template: "systemctl start {{sai_service(0, 'service_name', 'dnf')}}"
    validation:
      command: "systemctl is-active {{sai_service(0, 'service_name', 'dnf')}}"
      expected_output: "active"

  stop:
    description: "Stop service via systemctl"
    template: "systemctl stop {{sai_service(0, 'service_name', 'dnf')}}"
    validation:
      command: "systemctl is-active {{sai_service(0, 'service_name', 'dnf')}}"
      expected_output: "inactive"

  restart:
    description: "Restart service via systemctl"
    template: "systemctl restart {{sai_service(0, 'service_name', 'dnf')}}"

  enable:
    description: "Enable service auto-start"
    template: "systemctl enable {{sai_service(0, 'service_name', 'dnf')}}"

  disable:
    description: "Disable service auto-start"
    template: "systemctl disable {{sai_service(0, 'service_name', 'dnf')}}"

  status:
    description: "Check service status"
    template: "systemctl status {{sai_service(0, 'service_name', 'dnf')}}"

  logs:
    description: "Show service logs"
    template: "journalctl -u {{sai_service(0, 'service_name', 'dnf')}} --no-pager -n 50"

  info:
    description: "Show package information"
    template: "dnf info {{sai_package(0, 'name', 'dnf')}}"

  search:
    description: "Search for packages"
    template: "dnf search {{sai_package(0, 'name', 'dnf')}}"

  list:
    description: "List installed packages"
    template: "rpm -qa | grep {{sai_package(0, 'name', 'dnf')}}"

  version:
    description: "Show package version"
    template: "rpm -q {{sai_package(0, 'name', 'dnf')}} --queryformat '%{NAME} %{VERSION}-%{RELEASE}'"
